{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:5000",  // Start on port 5000
            "webRoot": "${workspaceFolder}",
            "sourceMaps": true
        }
    ]
}
